<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuanstack.lottery.infrastructure.dao.StrategyDetailDao">

    <select id="queryStrategyDetailList" parameterType="java.lang.Long"
            resultType="com.yuanstack.lottery.infrastructure.po.StrategyDetail">
        SELECT strategy_id,
               award_id,
               award_name,
               award_count,
               award_surplus_count,
               award_rate,
               create_time,
               update_time
        FROM strategy_detail
        WHERE strategy_id = #{strategyId}
    </select>

    <select id="queryNoStockStrategyAwardList" parameterType="java.lang.Long" resultType="java.lang.String">
        SELECT award_id
        FROM strategy_detail
        WHERE strategy_id = #{strategyId}
          AND award_surplus_count = 0
    </select>

    <update id="deductStock" parameterType="com.yuanstack.lottery.infrastructure.po.StrategyDetail">
        UPDATE strategy_detail
        SET award_surplus_count = award_surplus_count - 1
        WHERE strategy_id = #{strategyId}
          AND award_id = #{awardId}
          AND award_surplus_count > 0
    </update>

</mapper>
